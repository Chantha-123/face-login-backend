spring:
  datasource:
    url: jdbc:mysql://localhost:3306/face_login_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    show-sql: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 8081

logging:
  level:
    com.facelogin: DEBUG

telegram:
  bot:
    token: "8461269511:AAFWHLEm_cAGUY7tR9F_rtdsCFEIsmnx-ww" # Replace with your Telegram bot token
  admin:
    chat-id: "789748955" # Replace with your Telegram admin chat ID
